{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Program",
			"preLaunchTask": "Debug",
			"request": "launch",
			"type": "coreclr",
			"program": "${workspaceFolder}/lib/Belin.PhpMinifier.dll",
			"args": ["${input:input}"]
		}
	],
	"inputs": [
		{
			"description": "The path to the input directory.",
			"id": "input",
			"type": "promptString"
		}
	]
}
